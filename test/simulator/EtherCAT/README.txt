This is a simulator for a motor controller which can
be controlled via TCP/IP.

Currently the simulator does basic motion, positioning and
homing.

It is listening on port 5000 and can be accessed by telnet.

